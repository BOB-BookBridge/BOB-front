'use client';
import styled from 'styled-components';

const ListingSearchBar = () => {
  return <Container>검색탭</Container>;
};

export default ListingSearchBar;

export const Container = styled.div`
  height: 60px;
  display: flex;
  background-color: black;
  align-items: center;
  justify-content: center;
`;
